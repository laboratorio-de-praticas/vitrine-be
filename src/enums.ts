// Evento Enums
export enum TipoEvento {
  INTERNO = 'Interno',
  EXTERNO = 'Externo'
}

export enum StatusEvento {
  ATIVO = 'ATIVO',
  INATIVO = 'INATIVO'
}

// Usuario Enums
export enum TipoUsuario {
  ADMIN = 'Admin',
  ATENDENTE = 'Atendente'
}

export enum StatusUsuario {
  ATIVO = 'ATIVO',
  INATIVO = 'INATIVO'
}